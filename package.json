{
  "name": "fieldweaver",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Local-first deterministic generative-art instrument with LUT-driven colour and behaviour logic.",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "serve": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs --open",
    "benchmark:sim": "node scripts/benchmark-sim.mjs",
    "benchmark:render": "node scripts/benchmark-render.mjs",
    "verify": "npm run check && npm test"
  }
}
