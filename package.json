{
  "name": "fieldweaver",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Local-first deterministic generative-art laboratory with a canonical fixed-step simulation kernel.",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "serve": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs --open",
    "benchmark:sim": "node scripts/benchmark-sim.mjs",
    "verify": "npm run check && npm test"
  }
}
