{
  "name": "fieldweaver",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "description": "Local-first deterministic generative-art instrument with versioned recipes and checkpoint-assisted timeline replay.",
  "engines": {
    "node": ">=22 <25"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "serve": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs --open",
    "benchmark:sim": "node scripts/benchmark-sim.mjs",
    "benchmark:render": "node scripts/benchmark-render.mjs",
    "verify": "npm run check && npm test"
  }
}
